package com.example.plohoystream.ui.settings

/** Panel nav state. [Root] shows the grouped list; the rest are pushed sub-screens. */
enum class SettingsRoute { Root, Destination, Video, Audio, Camera, About, Obs }
